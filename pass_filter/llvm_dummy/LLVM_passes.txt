概述了 LLVM 提供的优化功能。优化以 Pass 的形式实现，Pass 会遍历程序的一部分，以收集信息或转换程序。

分析
aa-eval：详尽别名分析精度评估器

basic-aa基本别名分析（无状态别名分析实现）

basiccg基本调用图构建

da依赖性分析

domfrontier：优势前沿构建

domtree：支配树建筑

dot-callgraph将调用图打印到“dot”文件

dot-cfg将函数的 CFG 打印到“dot”文件中

dot-cfg-only将函数的 CFG 文件打印到“dot”文件中（不包含函数体）。

dot-dom将功能优势树打印到“dot”文件中

dot-dom-only将函数的支配树打印到“dot”文件中（不包含函数体）。

dot-post-dom：将功能后优势树打印到“dot”文件

dot-post-dom-only：将功能后优势树打印到“dot”文件（不包含功能体）

globals-aa：全局变量的简单模/引用分析

instcount统计各种类型的Instructions

iv-users：归纳变量用户

kernel-infoGPU内核信息

lazy-value-info懒惰价值信息分析

lint静态检查 LLVM IR

loops自然循环信息

memdep内存依赖性分析

print<module-debuginfo>解码模块级调试信息

postdomtree：后支配树结构

print-alias-sets别名设置打印机

print-callgraph打印调用图

print-callgraph-sccs打印呼叫图的SCC

print-cfg-sccs：打印每个函数 CFG 的 SCC

function(print)将函数输出打印到标准错误输出

module(print)将模块打印到标准错误输出

regions检测单入口单出口区域

scalar-evolution标量演化分析

scev-aa基于标量演化的别名分析

stack-safety烟囱安全分析

变换通道

adce：积极消除死代码

always-inlinealways_inline函数内联

argpromotion将“按引用传递”的参数提升为标量

block-placement：轮廓引导式基本模块放置

break-crit-edges：打破上下文无关文法中的关键边

codegenprepare优化代码生成

constmerge合并重复的全局常量

dce：死代码消除

deadargelim：消除死论点

dse：死店清除

function-attrs推断函数属性

globaldce：全球死亡淘汰赛

globalopt全局变量优化器

gvn全局值编号

indvars规范化归纳变量

inline功能集成/内联

instcombine合并冗余指令

aggressive-instcombine组合表达模式

internalize：内化全局符号

ipsccp：过程间稀疏条件常量传播

normalize将 IR 转换为更易于区分的普通形式

jump-threading跳转线程

lcssa：闭环 SSA 表格通过

licm：循环不变码运动

loop-deletion删除死循环

loop-extract将循环提取到新函数中

loop-reduce：环路强度降低

loop-rotate旋转循环

loop-simplify规范化自然循环

loop-unroll展开循环

loop-unroll-and-jam展开和循环

lower-global-dtors：降低全局析构函数

lower-atomic降低原子固有属性至非原子形式

lower-invoke降低调用次数，以用于无回溯代码生成器

lower-switch：降低SwitchInsts 到分支

mem2reg：将内存提升到寄存器

memcpyopt：MemCpy 优化

mergefunc合并函数

mergereturn统一函数出口节点

partial-inliner：部分内衬

reassociate重新关联表达式

rel-lookup-table-converter相对查找表转换器

reg2mem将所有值降级为堆叠槽

sroa：聚合的标量替换

sccp稀疏条件常数传播

simplifycfg简化 CFG

sink代码沉没

simple-loop-unswitch：解除循环

strip：从模块中移除所有符号

strip-dead-debug-info：去除未使用符号的调试信息

strip-dead-prototypes：删除未使用的功能原型

strip-nondebug从模块中移除除dbg符号之外的所有符号

tailcallelim尾部呼叫消除

公用通行证

deadarghaX0r：死论证破解（仅限 BUGPOINT 使用；请勿使用）

extract-blocks从模块中提取基本块（用于 bugpoint）

instnamer为匿名指令分配名称

verify模块验证器

view-cfg查看函数的 CFG

view-cfg-only查看函数的 CFG（不包含函数体）

view-dom查看功能优势树

view-dom-only查看功能优势树（不含功能体）

view-post-dom查看功能后优势树

view-post-dom-only查看后优势功能树（不含功能体）

transform-warning：报告遗漏的强制转换